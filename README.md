# ECG Regression Models with Stacking

## Overview
This project builds regression models to predict the `TARGET` variable from ECG measurements. The notebook trains several supervised learning models and evaluates their performance on training and test data.  

The project also builds an ensemble model using stacking, which combines predictions from multiple models to improve prediction accuracy.

## Models Used
- Decision Tree Regressor  
- Random Forest Regressor  
- Gradient Boosting Regressor  
- Final ensemble: Stacking Ensemble Learning  

Performance is evaluated using **Mean Squared Error**.

## Dataset
- Input files: `train.csv` and `test.csv` in the `data/` folder  

**Description:**  
- The training dataset contains feature variables and the `TARGET` column  
- The `TARGET` column represents the value that the models predict  
- The test dataset is used to evaluate prediction performance  

## Workflow
- Load the datasets  
- Separate features and target variable  
- Train individual models: Decision Tree, Random Forest, Gradient Boosting  
- Evaluate predictions using Mean Squared Error  
- Build a stacked ensemble model  
- Generate predictions using the stacked model  
- Save predictions from all models to a single Excel file  

## Output
- Output file: `data/output_regression.xlsx`  
- Sheets inside the file:  
  - `DecisionTree`  
  - `RandomForest`  
  - `GradientBoosting`  
  - `Stacking`  
- Each sheet contains the predicted `TARGET` values generated by the corresponding model  

## Project Structure
```
project_folder
│
├── data
│   ├── train.csv
│   ├── test.csv
│   └── output_regression.xlsx
│
├── ecg_regression_stacking.ipynb
└── README.md
```

## Requirements
- Python 3  
- Libraries: `pandas`, `numpy`, `scikit-learn`, `vecstack`, `openpyxl`  

Install dependencies:
```
pip install pandas numpy scikit-learn vecstack openpyxl
```

## How to Run
1. Open the notebook: `ecg_regression_stacking.ipynb`  
2. Run all cells in sequence  
3. After execution, the output file will appear in the `data/` folder  

## Output Verification
- Confirm that `data/output_regression.xlsx` exists  
- Open the Excel file to view prediction results from each model  
